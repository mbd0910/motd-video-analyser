# MOTD Analyser Configuration

# Data paths
fixtures:
  directory: data/fixtures

teams:
  path: data/teams/premier_league.json

episodes:
  manifest_path: data/episodes/episode_manifest.json

# Cache
cache:
  directory: data/cache

# Logging
logging:
  level: INFO
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
