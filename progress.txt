#19 Codebase reset: delete old pipeline, establish new skeleton — DONE
  - Deleted old modules (scene_detection, ocr, llm, pipeline, config, utils, validation)
  - Removed heavy ML deps (EasyOCR, PySceneDetect, OpenCV, torch, etc.)
  - New Pydantic data contracts: TranscriptSegment, Transcript, Fixture, Score, Segment, MatchCoverage, EpisodeAnalysis
  - FixtureProvider interface + FileFixtureProvider
  - Module stubs: transcriber, analyser, publisher, downloader, pipeline
  - New CLI: download, transcribe, analyse, publish, run
  - Retained: audio_extractor.py, domain data, domain docs
  - 22 tests, clean mypy

#20 Tracer bullet: local video → analysis JSON on disk — DONE
  - Transcriber: chunked ffmpeg→MP3 segments, OpenAI Whisper API (verbose_json), timestamp offsets
  - Analyser: prompt builder (transcript + fixtures), Claude via `claude -p`, JSON parsing with fence stripping
  - CLI: `transcribe` and `analyse` commands wired up with caching, --force, --episode-id
  - Added openai dependency
  - 22 new tests (11 transcriber, 11 analyser), 44 total, clean mypy + ruff

#21 Publish analysis JSON to Cloudflare R2 — DONE
  - Publisher module: R2Client protocol, CloudflareR2Client (boto3 S3-compatible), publish()
  - Serialises EpisodeAnalysis to JSON, uploads to episodes/{episode_id}.json
  - Episode index (episodes/index.json): read-modify-write, upsert (no duplicates)
  - R2 credentials via env vars (R2_BUCKET, R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY)
  - CLI: `python -m motd publish EPISODE_ID` loads cached analysis and publishes
  - Added boto3 dependency, boto3-stubs for type checking
  - 11 new tests (2 index entry, 4 index update, 5 publish integration), 55 total, clean mypy + ruff

#23 iPlayer downloader: yt-dlp acquisition — DONE
  - Downloader module: download(url_or_id, output_dir) → DownloadResult(video_path, episode_id)
  - Accepts iPlayer URL or programme ID (auto-normalised to full URL)
  - Two-step yt-dlp subprocess: --dump-json for metadata, then download
  - Episode ID derived from broadcast date (release_date → upload_date fallback)
  - Season derivation: Aug–Dec → YYYY-(YY+1), Jan–Jul → (YYYY-1)-YY
  - Skips download if video already exists at target path
  - DownloadError for clear failure messages
  - CLI: `python -m motd download URL_OR_ID [--output-dir DIR]`
  - 19 new tests (3 URL, 3 date parsing, 5 season, 2 episode ID, 6 integration), 74 total, clean mypy + ruff

#24 End-to-end CLI: wire all stages with logging — DONE
  - Pipeline orchestrator: Download (optional) → Transcribe → Analyse → Publish
  - Cache-aware: skips transcription if transcript.json exists, writes analysis.json
  - --skip-to flag resumes from a specific stage (requires --episode-id)
  - --force flag re-processes even when cached outputs exist
  - Error handling preserves completed stage outputs (transcript survives analysis failure)
  - Per-stage timing logs (start, completion, duration)
  - PipelineError for clear user-facing error messages (no fixtures, missing inputs)
  - CLI `run` command wired to pipeline with PipelineError → sys.exit(1)
  - 13 new tests (3 full pipeline, 1 URL path, 2 validation, 2 skip-to, 2 error handling, 2 caching, 1 no-fixtures), 87 total, clean mypy + ruff

#22 Website MVP: display episode data from R2 — DONE
  - Astro static site scaffolded at site/
  - Episode list page (/): fetches index.json from R2, cards sorted by date, match count + season
  - Episode detail page (/episode?id=...): running order table with highlights/studio durations, scores, venues, notes
  - Loading, empty, and error states on both pages
  - Mobile-responsive: table hides venue/notes columns on small screens
  - R2 URL configurable via PUBLIC_R2_URL env var (falls back to /data/ for local dev)
  - TypeScript types mirroring Python Pydantic models
  - Local test fixture data in public/data/episodes/ for development
  - Astro build succeeds, 87 Python tests still pass
