"""
Default configuration values for MOTD Analyser.

These defaults are used as fallback values when configuration is missing or incomplete.
"""

# Scene Detection Defaults
DEFAULT_THRESHOLD = 30.0
DEFAULT_MIN_SCENE_DURATION = 3.0
DEFAULT_DETECTOR_TYPE = "content"
